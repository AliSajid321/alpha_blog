<h1>Categories</h1>

<%= will_paginate @categories %>

<table>
    <thead>
        <tr>
            <th>Category Name</th>
            
        </tr>
    <thead>
     <tbody>
        <% @categories.each do |category| %>
            <tr>
                <td><%= category.name %></td>
                
            </tr>  
           
        <% end %>

        
    </tbody>

</table>

<%= will_paginate @categories %>